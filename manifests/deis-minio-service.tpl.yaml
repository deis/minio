apiVersion: v1
kind: Service
metadata:
  # Give this a useful name.
  name: deis-minio
  namespace: {{.SVC_NAMESPACE}}
  labels:
    heritage: deis
    release: 0.0.0
spec:
  ports:
    - port: 9000
      targetPort: 9000
      name: s3
      protocol: TCP
  selector:
    app: deis-minio
