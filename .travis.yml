language: generic
branches:
  only:
    - master
cache:
  directories:
    - vendor
services:
  - docker
sudo: required
install:
  - make bootstrap
script:
  - make test
deploy:
  provider: script
  on:
    all_branches: true
  script: _scripts/deploy.sh
